class TableTennisConfig {
  static const breakTimer = 60; // seconds (for regular breaks)
  static const setGap = 60; // seconds (between sets)
  static const gameGap = 120; // seconds (between games)
  static const timeoutTimer = 60; // seconds

  static const subscribeUrl = "https://jamiecee.org/app";
}
